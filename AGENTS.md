# Agent Instructions

- Never run development server commands such as `npm run dev`, `next dev`, `vite`, or similar.
- Never run build commands such as `npm run build`, `next build`, or similar.
- Use non-server verification commands where appropriate, such as type checking or targeted static checks.
