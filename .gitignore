node_modules
.next
out
.turbo
.env*.local
*.log
.DS_Store

# build artifacts
tsconfig.tsbuildinfo

# reacher CLI binary — local testing only; production runs the Docker backend
reacher-binary/

# local tooling (Claude Code harness, agent configs, skill locks)
.claude/
.agents/
skills-lock.json
